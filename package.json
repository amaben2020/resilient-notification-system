{
  "name": "notif-system",
  "version": "1.0.0",
  "description": "Payment -> SNS fan-out -> SQS -> Lambda workers",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js",
    "db:studio": "node --env-file=.env ./node_modules/.bin/drizzle-kit studio",
    "build:workers": "node scripts/build-workers.mjs",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "node --env-file-if-exists=.env src/config/migrate.js",
    "smoke": "node scripts/smoke-test.mjs",
    "test:watch": "vitest",
    "test:integration": "node --env-file-if-exists=.env ./node_modules/.bin/vitest run --config vitest.integration.config.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-sns": "^3.1135.0",
    "@neondatabase/serverless": "^1.1.0",
    "drizzle-orm": "^0.45.2",
    "express": "^5.2.1",
    "pino": "^10.3.1",
    "prom-client": "^15.1.3",
    "serverless-http": "^4.0.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "esbuild": "^0.28.2",
    "pino-pretty": "^13.1.3",
    "supertest": "^7.2.2",
    "vitest": "^5.0.1"
  }
}
